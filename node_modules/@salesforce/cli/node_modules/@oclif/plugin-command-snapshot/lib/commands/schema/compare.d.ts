import { Operation } from 'just-diff';
import SnapshotCommand from '../../snapshot-command.js';
export type SchemaComparison = Array<{
    op: Operation;
    path: (number | string)[];
    value: unknown;
}>;
export default class SchemaCompare extends SnapshotCommand {
    static flags: {
        filepath: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
    };
    run(): Promise<SchemaComparison>;
    private readExistingSchema;
}
