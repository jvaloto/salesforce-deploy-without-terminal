import { AgentTestResultsResponse } from '@salesforce/agents';
export declare function truncate(value: number, decimals?: number): string;
export declare function readableTime(time: number, decimalPlaces?: number): string;
export declare function humanFormat(results: AgentTestResultsResponse): string;
export declare function handleTestResults({ id, format, results, jsonEnabled, outputDir, }: {
    id: string;
    format: 'human' | 'json' | 'junit' | 'tap';
    results: AgentTestResultsResponse | undefined;
    jsonEnabled: boolean;
    outputDir?: string;
}): Promise<void>;
