import { createPrompt, type Theme } from '@inquirer/core';
import type { PartialDeep } from '@inquirer/type';
type ConfirmConfig = {
    message: string;
    default?: boolean;
    transformer?: (value: boolean) => string;
    theme?: PartialDeep<Theme>;
};
declare const yesNoOrCancel: ReturnType<typeof createPrompt<boolean | 'cancel', ConfirmConfig>>;
export default yesNoOrCancel;
