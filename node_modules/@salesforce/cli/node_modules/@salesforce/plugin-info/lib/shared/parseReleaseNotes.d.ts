import { marked } from 'marked';
export declare const parseReleaseNotes: (notes: string, version: string, baseUrl: string) => marked.Token[];
declare const _default: {
    parseReleaseNotes: (notes: string, version: string, baseUrl: string) => marked.Token[];
};
export default _default;
