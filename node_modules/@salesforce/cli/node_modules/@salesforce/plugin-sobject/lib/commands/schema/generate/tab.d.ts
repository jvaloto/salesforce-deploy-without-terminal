import { SfCommand } from '@salesforce/sf-plugins-core';
import type { CustomTab } from '@salesforce/types/metadata';
export type GenerateTabResult = {
    tab: Pick<CustomTab, 'customObject' | 'motif'>;
    path: string;
};
export default class GenerateTab extends SfCommand<GenerateTabResult> {
    static readonly summary: string;
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresProject = true;
    static readonly aliases: string[];
    static readonly deprecateAliases = true;
    static readonly flags: {
        object: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
        directory: import("@oclif/core/interfaces").OptionFlag<string, import("@oclif/core/interfaces").CustomOptions>;
        icon: import("@oclif/core/interfaces").OptionFlag<number, import("@oclif/core/interfaces").CustomOptions>;
    };
    run(): Promise<GenerateTabResult>;
}
