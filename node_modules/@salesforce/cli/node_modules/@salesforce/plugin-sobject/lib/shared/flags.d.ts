export declare const labelValidation: (label: string) => Promise<string>;
/**
 * Validates that a path contains /objects/ and at least one directory beyond that
 *
 * @param path: relative or full path
 * @returns true if valid
 */
export declare const isObjectsFolder: (path: string) => Promise<string>;
/**
 * Validates that a path ends in /tabs
 *
 * @param path: relative or full path
 * @returns true if valid
 */
export declare const isTabsFolder: (path: string) => Promise<string>;
