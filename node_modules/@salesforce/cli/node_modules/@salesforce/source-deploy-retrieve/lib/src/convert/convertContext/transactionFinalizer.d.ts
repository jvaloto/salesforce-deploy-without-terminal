import { WriterFormat } from '../types';
export declare abstract class ConvertTransactionFinalizer<T> {
    protected abstract transactionState: T;
    abstract finalize(defaultDirectory?: string): Promise<WriterFormat[]>;
}
