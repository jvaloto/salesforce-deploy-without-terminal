import { SourceComponent } from '../../resolve/sourceComponent';
import { ToSourceFormatInput, WriteInfo } from '../types';
import { DefaultMetadataTransformer } from './defaultMetadataTransformer';
export declare class LabelsMetadataTransformer extends DefaultMetadataTransformer {
    /** CustomLabels file => Array of CustomLabel WriteInfo (one for each label) */
    toSourceFormat({ component, mergeSet }: ToSourceFormatInput): Promise<WriteInfo[]>;
}
export declare class LabelMetadataTransformer extends DefaultMetadataTransformer {
    toMetadataFormat(component: SourceComponent): Promise<WriteInfo[]>;
}
